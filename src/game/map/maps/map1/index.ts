import { TDMap } from "../types";
import { config } from "./config";
import { decorations } from "./decoration";
import { rounds } from "./rounds";

export const map1: TDMap = { config, decorations, rounds };
